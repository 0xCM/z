<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>$(FrameworkMoniker)</TargetFramework>
  </PropertyGroup>

  <Import Project="$(ProjectProps)shells.props"/>
  <Import Project="$(ProjectProps)usings.props"/>
  <Import Project="$(ImportDefs)microsoft.cmdline.props"/>
  <Import Project="$(ProjectProps)libs.props"/>

  <ItemGroup>
    <ProjectReference Include="$(CmdSpecs)" />
    <ProjectReference Include="$(Rules)" />
  </ItemGroup>

</Project>
