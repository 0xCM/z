//-----------------------------------------------------------------------------
// Copyright   :  (c) Chris Moore, 2020
// License     :  MIT
//-----------------------------------------------------------------------------
namespace Z0
{
    /// <summary>
    /// A <see cref='byte'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E8u : byte { }

    /// <summary>
    /// A <see cref='sbyte'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E8i : sbyte { }

    /// <summary>
    /// A <see cref='short'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E16i : short { }

    /// <summary>
    /// A <see cref='ushort'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E16u : ushort { }

    /// <summary>
    /// A <see cref='int'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E32i : int { }

    /// <summary>
    /// A <see cref='uint'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E32u : uint { }

    /// <summary>
    /// A <see cref='long'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E64i : long { }

    /// <summary>
    /// A <see cref='ulong'/> refinement that is both vacuous and canonical
    /// </summary>
    public enum E64u : ulong { }
}