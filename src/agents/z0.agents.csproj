<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="$(ProjectProps)\usings.props"/>
  <Import Project="$(ProjectProps)\packages.microsoft.extensions.hosting.props" />

  <ItemGroup>
     <ProjectReference Include="$(Events)"/>
  </ItemGroup>
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>agents</AssemblyName>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>
</Project>