<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="$(ProjectProps)\usings.props"/>

  <ItemGroup>
     <ProjectReference Include="$(Native)"/>
     <ProjectReference Include="$(Process)"/>
  </ItemGroup>
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>agents</AssemblyName>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>
</Project>